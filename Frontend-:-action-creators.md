* should return `response` after dispatch.
* use `async` and `await`
* One Action should do only one thing.
* Create another action to compose mulitple actions
* `async` action should contain `try catch` block
